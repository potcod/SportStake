import 'package:flutter/material.dart';

class PickEmPage extends StatelessWidget {
  const PickEmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Pick Em Page'));
  }
}